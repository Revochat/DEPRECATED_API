import {updateName} from "./channels.name.update"
import {updatePermissions} from "./channels.permissions.update"

export const update = {
    channel_name: updateName,
    permissions: updatePermissions
}
