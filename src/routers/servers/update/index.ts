import {iconUpdate} from './servers.icon.update';
import {nameUpdate} from './servers.name.update';
import {permissionsUpdate} from './servers.permissions.update';

export const update = {
    icon: iconUpdate,
    name: nameUpdate,
    permissions: permissionsUpdate
}